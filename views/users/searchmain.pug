extends ../layout/common.pug
block content
    h2 Users
    ul
        each user in users
            li
                span= user.name
                span &nbsp
                a(href='/users/'+ user.id) View